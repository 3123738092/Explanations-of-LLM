# AttnLRP 参数归因可视化与分析指南 (README_Visualization.md)

## 1. 概述 (Overview)
在大模型参数归因任务中，算法输出的是与模型权重形状一致的巨量张量（如 LLaMa-7B 会产生 70 亿个分数）。本指南旨在提供一套标准的可视化方案，将这些“原始数据”转化为可分析的“神经科学见解”。

本指南应配合 `ATTN_LRP_PARAMETER_ATTRIBUTION_SPEC.md` (算法修改指南) 共同使用。

---

## 2. 方案一：组件级聚合热力图 (Component-level Aggregation)
**适用场景：** 寻找负责特定逻辑（如语法解析、实体关联）的注意力头。

### 2.1 聚合逻辑
不直接观察 $W_q, W_k, W_v$ 的单个权重，而是按 **注意力头 (Head)** 进行降维：
1. **绝对值求和：** 对第 $l$ 层第 $h$ 个头的所有参数贡献值取绝对值并求和。
2. **矩阵构建：** 得到一个形状为 `[Layers, Heads]` 的二维矩阵（如 $32 \times 32$）。

### 2.2 可视化表现
- **横轴：** 网络层数 (Layer Index)
- **纵轴：** 注意力头编号 (Head Index)
- **颜色深度：** 代表该头在该样本下的“参与度”。
- **结论：** 能够直观发现某些特定层（如中深层）的特定头对预测结果起到了主导作用。

---

## 3. 方案二：层级宏观贡献趋势 (Layer-wise Trend Analysis)
**适用场景：** 分析模型处理信息时的流水线特征（Pipeline Analysis）。

### 3.1 聚合逻辑
将每一层中所有参数的贡献值进行总和统计，并区分模块类型：
1. **Attention 贡献度：** 该层所有注意力相关参数的总得分。
2. **MLP 贡献度：** 该层所有全连接层相关参数的总得分。

### 3.2 可视化表现
- **折线图：** X 轴为层数，Y 轴为归一化后的相关性得分。
- **典型现象：**
    - **浅层 Attention 峰值：** 正在处理输入文本的表面语法。
    - **中层 MLP 峰值：** 模型正在从权重参数中提取事实知识（Knowledge Retrieval）。
    - **深层 Attention 峰值：** 正在为最终输出进行词汇预测组合。

---

## 4. 方案三：扰动验证曲线 (Faithfulness Perturbation)
**适用场景：** 证明计算出的参数贡献值是“真实且正确”的（算法验证）。

### 4.1 算法验证逻辑
1. **排序：** 将模型所有参数按计算出的贡献度分数从高到低排序。
2. **消融 (Ablation)：** 按照百分比（如 0.01%, 0.1%, 1%...）依次将排名最高的参数置为 0（剪枝）。
3. **监测：** 记录目标 Logit 值随参数剔除比例的变化。

### 4.2 可视化表现
- **X 轴：** 剔除参数的比例。
- **Y 轴：** 目标 Token 的 Logit 值。
- **成功标准：** 曲线呈现**断崖式下跌**。这证明算法精准定位了那 0.1% 最关键的参数。

---

## 5. 工程实现建议
- **数据降维：** 建议在 GPU 上直接完成 `.sum()` 聚合操作，只将聚合后的标量传回 CPU，以避免内存溢出。
- **对比可视化：** 同时计算目标 Token 和对比 Token（例如预测“巴黎”时，对比“伦敦”）的参数归因，寻找两者的**差值矩阵**。
- **归一化处理：** 在画热力图前，建议对整张图进行 `min-max` 归一化，将颜色映射在 `[0, 1]` 区间。

## 6. 已实施的改动：两种参数归因图层级对比

### 6.1 问题
方案二中 `layer_component_abs` 是对每层每组件所有参数的 `|W ⊙ grad|` 求和。MLP 参数量 (~4.7M) 是 Attention (~2.4M) 的 2 倍，LayerNorm (1.5K) 的 3000 倍。全局 max 归一化后，小参数量的组件被压扁不可见，跨组件对比无意义。

### 6.2 输出两份图，各解决不同问题

**Line chart（折线图）— 看形状**：`save_layer_param_line()`
- 每条组件曲线用**自身最大值**独立归一化到 [0,1]
- 三条线都在同一区间，可直接比较各组件在哪些层达到峰值
- 回答："attention 在哪层最活跃？mlp 在哪层最活跃？"

**Block（热图）— 看形状 + 量级**：`save_layer_parameter_trend()`
- `[12层 × 3模块]` 热图，每列独立 min-max 归一化
- **颜色**（列内归一化）：该模块在哪些层活跃（深紫→亮黄）
- **数字**（原始 `Σ|W×grad|`）：跨模块的绝对量级对比（MLP ~400 vs LN ~10）
- 一张图同时回答"峰位在哪"（看颜色）和"量级多大"（看数字）
- 配色用 viridis（sequential），不用 bwr（bwr 暗示有 0 中心）

### 6.3 输出目录
| 目录 | 内容 |
|---|---|
| `figures/parameter_layers_line/` | 折线图，per-component 自归一化 |
| `figures/parameter_layers_block/` | 热图，列内 min-max + 格内数字 |
| ~~`figures/parameter_layers/`~~ | 已移除，不再使用 |

### 6.4 相关代码位置
| 文件 | 改动 |
|---|---|
| `src/explain/attnlrp_gpt2_efficient.py`:`_collect_parameter_relevance()` | 新增 `layer_component_numel` 统计 + `layer_component_abs_mean` 计算 |
| `src/visualize/heatmap.py`:`save_layer_param_line()` | 新增：折线图，per-component 自归一化 |
| `src/visualize/heatmap.py`:`save_layer_parameter_trend()` | 改为热图：列内 min-max + 格内 raw 数字 |
| `main.py` | 两图分别调用，输出到 line/ 和 block/ 目录 |

### 6.5 运行
```bash
cd /mnt/d/桌面/nlp_new/Explanations-of-LLM
conda activate exp
python main.py --config configs/gpt2_efficient.yaml
```
确保配置中 `parameter_attribution.enabled: true`。也可用另两个 config（`gpt2_efficient_finetuned_sciq.yaml` / `gpt2_efficient_finetuned_squad.yaml`）对微调模型跑对比。
